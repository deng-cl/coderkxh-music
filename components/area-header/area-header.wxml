<!--components/area-title/area-title.wxml-->
<view class="head">
    <view class="title">{{ title }}</view>
    <view class="more" wx:if="{{ hasMore }}" bind:tap="onMoreTap">
        <text class="text">更多</text>
        <van-icon name="arrow" />
    </view>
</view>

